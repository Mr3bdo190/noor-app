name: noor
description: Noor Islamic App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  adhan: ^2.0.1          # لحساب مواقيت الصلاة بدقة
  geolocator: ^10.1.0    # لتحديد الموقع GPS
  flutter_qiblah: ^2.0.0 # للبوصلة
  shared_preferences: ^2.2.2 # لحفظ النقط والأذكار

flutter:
  uses-material-design: true
